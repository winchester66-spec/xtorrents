# Adicione ao seu .htaccess para cache das capas locais:
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/webp "access plus 1 month"
</IfModule>

<IfModule mod_headers.c>
  <FilesMatch "\.webp$">
    Header set Cache-Control "public, max-age=2592000"
  </FilesMatch>
</IfModule>
